# Force Railway rebuild - Jan 25, 2026
[phases.build]
cmds = ["npm install --production --no-audit --no-fund"]

[start]
cmd = "node server-nexpart-with-shop-settings.js"

[healthcheck]
path = "/health"
interval = "30s"
timeout = "10s"
retries = 3
