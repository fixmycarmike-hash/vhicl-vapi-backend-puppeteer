[phases.build]
cmds = ["npm install --production --no-audit --no-fund"]

[phases.start]
cmds = ["node server-nexpart-with-shop-settings.js"]

[start]
cmd = "node server-nexpart-with-shop-settings.js"

[healthcheck]
path = "/health"
interval = "30s"
timeout = "10s"
retries = 3
